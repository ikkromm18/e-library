# Plan: Fix Upload Gambar, Pagination, Import Buku, Landing Login, & Optimasi Query

Tanggal: 2026-08-06

## Urutan eksekusi
1. `php artisan storage:link` + tambah script `post-autoload-dump` di composer.json.
2. Landing route `/` → redirect ke `/dashboard` (auth) / `/login` (guest); hapus `welcome.blade.php`.
3. Fix upload cover Buku + logo Setting (WithFileUploads, rule 1MB, preview, display).
4. Kolom No + pagination + perPage selector + `preserveScroll` (5 master list).
5. Laporan rows: No + pagination; cart peminjaman & detail pengembalian: kolom No.
6. Import Buku + template + routes + SFC + tombol akses di header buku/anggota.
7. N+1 fixes: buku list `withCount`, BukuExport, TransaksiExport, hasilBuku peminjaman.
8. Index DB migration.
9. Tests.

## 1. Storage link
- `composer.json`: di `scripts.post-autoload-dump` tambah `"@php artisan storage:link"`.
- Jalankan `php artisan storage:link` sekali (link public/storage).

## 2. Landing
- `routes/web.php`:
  ```php
  Route::get('/', function () {
      return redirect(auth()->check() ? '/dashboard' : '/login');
  });
  ```
- Hapus `resources/views/welcome.blade.php`.

## 3. Upload gambar
### Buku (`resources/views/components/buku/buku.php`)
- `use Livewire\WithFileUploads;`.
- Rule di `save()`: `'cover' => 'nullable|image|mimes:jpeg,png,jpg,gif,webp|max:1024'`.
- Saat edit tampilkan cover lama; preview baru via `temporaryUrl()`.

### `buku.blade.php`
- Form: `@if ($cover) <img src="{{ $cover->temporaryUrl() }}"> @endif`; saat edit + cover lama tampil.
- Kolom tabel: thumbnail `<img src="{{ $buku->cover ? asset('storage/'.$buku->cover) : placeholder }}">`.
- Modal detail: tampilkan gambar.
- Header: tombol Import → `/buku/import`.

### Setting (`setting.php`/`blade`)
- Rule: `'logo' => 'nullable|mimes:jpeg,png,jpg,gif,webp,svg|max:1024'`.
- Preview `temporaryUrl()`.
- `navigation.blade.php` & `guest.blade.php`: `$logo = \App\Models\Setting::get('logo')`; bila ada tampil `<img src="{{ asset('storage/'.$logo) }}">`, fallback `<x-application-logo />`.

## 4. No + Pagination + perPage + preserveScroll
Komponen: buku, anggota, kategori, rak, pengguna.
- Kolom No: `{{ $list->firstItem() + $loop->index }}`.
- `public $perPage = 10;` + `public function updatedPerPage(): void { $this->resetPage(); }`.
- `render`: `->paginate($this->perPage)` (ganti `->get()` di kategori/rak/pengguna; tambah WithPagination di mereka).
- Selector di blade dekat search:
  ```blade
  <select wire:model.live="perPage">
      @foreach ([10,25,50,100] as $n)<option value="{{ $n }}">{{ $n }}</option>@endforeach
  </select>
  ```
- `{{ $list->links() }}` + root komponen `<div ... wire:preserveScroll>`.
- `@empty` row colspan ikut kolom No.

## 5. Laporan & transaksi
### `laporan.php`
- Tambah `$perPage = 10`, `$page = 1`; `muatData()` buat collection penuh (export butuh). 
- Pagination tampilan: slice `$rows->forPage($page, $perPage)`; `$paginated` di render.
- `updatedPerPage` reset `$page`.
- `laporan.blade.php`: kolom No pertama via `$loop->iteration` pada baris ter-paginasi; `$rows->links()`.
- Transaksi: `TransaksiExport` default → `->withCount('details')`, pakai `$p->details_count`.

### `peminjaman.blade.php` cart & `pengembalian.blade.php` detail
- Tambah kolom `<th>No</th>` & `{{ $loop->iteration }}` (list kecil non-paginasi).

## 6. Import Buku
### `app/Imports/BukuImport.php`
- `ToModel + WithHeadingRow + SkipsOnFailure + Importable` (pola `AnggotaImport` 3.1.69).
- Lookup map kategori (by nama) & rak (by kode) dibangun di `__construct`.
- `model(array $row)`:
  - kategori/rak wajib cocok; bila tak cocok → skip + counter.
  - kode: pakai `$row['kode']` bila unik non-blank, selain itu `Buku::buatKode()`.
  - jumlah_eksemplar default 1; status `aktif`/`tidak`.
  - counter `imported`/`skipped`/`errors`.
- `rules()` validasi inti.

### `app/Exports/BukuTemplateExport.php`
- `FromCollection + WithHeadings`; headings: `['kode','isbn','judul','kategori','rak','pengarang','penerbit','tahun','bahasa','jumlah_eksemplar','status']`; 1 baris contoh.

### Routes
- `/buku/import` → `view('buku.import')` (mount livewire).
- `/buku/import/template` → `Excel::download(new BukuTemplateExport, 'template_buku.xlsx')`.
- `resources/views/buku/import.blade.php` render `<livewire:buku-import />`.

### SFC `resources/views/components/buku/buku-import/{buku-import.php,buku-import.blade.php}`
- Salin pola member-import: `WithFileUploads`, rule `xlsx,xls,csv max:10240`, `preview()` DB beginTransaction + rollback, `confirmImport()` commit, statistik card.
- `anggota.blade.php` header tombol Import → `/anggota/import`.

## 7. N+1 fixes
- `buku.php` `queryBuku()`: `Buku::with(['kategori','rak'])->withCount(['detailsAktif as stok_dipakai'])`.
  - blade: `{{ ($buku->jumlah_eksemplar - (int) $buku->stok_dipakai) }} / {{ $buku->jumlah_eksemplar }}`.
- `BukuExport.collection()`: sama `withCount`, ganti `$b->stokTersedia()`.
- `TransaksiExport` default: `Peminjaman::with(['anggota','petugas'])->withCount('details')`, pakai `$p->details_count`.
- `peminjaman.php` `hasilBuku`: `Buku::withCount('detailsAktif as stok_dipakai')`, blade `stok_dipakai`.

## 8. Index migration
- `2026_08_06_000001_add_indexes.php`:
  - `anggota`: index `nama`, `status`.
  - `bukus`: index `judul`, `status`.
  - `peminjaman_details`: index `buku_id`, `tanggal_kembali`.

## 9. Tests (`tests/Feature/`)
- `LandingRedirectTest`: guest `/` → `/login`; auth → `/dashboard`.
- `BukuImportTest`: valid import, duplikat skip, invalid kategori skip, template route 200, preview tidak persist.
- `BukuCoverTest`: upload cover (NamedUploadedFile), tersimpan di disk public, preview URL, rule >1MB tolak.
- `SettingLogoTest`: upload logo, logoPath set; navbar tampil logo bila ada.
- `PaginationNoTest`: perPage ubah reset page; No column ada; `preserveScroll` ada; kategori/rak/pengguna paginate.
- N+1: query log list buku 1 halaman ≤ 3 query.

## Verifikasi akhir
- `vendor/bin/pint`
- `composer test`
- `npm run build`
- `composer dev` smoke manual (toggle, upload, pagination, import, login redirect).
